# Copy this file to `.env` and fill in the real values.
# `.env` is gitignored — never commit it.

# === App under test ===
BASE_URL=https://oneshopqa.com.dev.nmg-platform.com/

# === Optional test credentials (only if a spec needs to log in) ===
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# === Optional: Monday.com API (not required for the standard JS flow) ===
# Generate at: avatar → Developers → My Access Tokens
MONDAY_API_TOKEN=
MONDAY_BOARD_ID=18395174855
